#include <stdio.h>
int main (){
    char p1[30], p2[30];
    printf("informe palavra 1:");
    gets(p1);
    printf("informe palavra 2:");
    gets(p2);
    if(strcmp (p1,p2));
    printf("são iguais");
    
    return 0;
 
}
